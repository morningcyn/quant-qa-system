

把重型中台砍掉，做成一个**本地桌面端 / 部门轻量 Web 工具**：只要把导出的聊天文本粘进去或传文件，大模型自动按 S/D 规则打分，生成包含**雷达图、失分归因、AI 话术重写（金牌 SOP 修复）、改进建议**的精美报告，并按员工沉淀历史档案。
同时桌面应用要调用的大模型的apikey在桌面端的我的，我的模型进行配置apikey（绝不烧开发者的token）
---

### 一、 系统功能与交互架构

```
[员工管理模块] 
  ├── 添加/管理下属（姓名、工号、当前级别/模板）
  └── 下属个人主页（历史会话库、历史评分趋势图、薄弱项统计）
        │
        └── [上传新会话] (支持粘贴 / TXT / CSV / JSON 导出记录)
              │
              ▼
[大模型智能质检引擎 (API)]
  ├── 1. 结构化解析：识别 [客] / [助] 轮次与上下文语境
  ├── 2. 规则评分计算：严格执行 D端(55分) + S端(45分) 与 59分熔断机制
  ├── 3. 诊断归因：定位扣分具体话术轮次
  ├── 4. 黄金话术修复：AI 针对原对话生成“高分改写对比版”
  └── 5. 诊断建议：生成 1~3 条即学即用的个性化改进动作
              │
              ▼
[可视化报告与归档]
  ├── 交互式仪表盘：总分 + 黄灯标签 + S/D 细项雷达图
  ├── 左右双屏复盘：左侧原话高亮标错 vs 右侧 AI 黄金改写
  ├── 一键导出（PDF / 高清长图）
  └── 自动归档至该员工历史记录库

```

---

### 二、 核心技术方案选型（最适合快速落地的方案）

根据你的需求，这套方案彻底转为**纯本地运行的单机桌面软件**：零服务器成本、零运维开销，数据全部保存在本地，兼具现代 SaaS 级质感与强大的 AI 编排能力。

---

#### 1. 新技术选型矩阵

| 分层 | 选型技术 | 核心价值与职责 |
| --- | --- | --- |
| **桌面客户端宿主** | **Tauri (推荐)**  | 跨平台打包（Windows `.exe` / macOS `.dmg`），轻量低内存占用（Tauri 安装包仅约 10MB~20MB，比 Electron 更流畅）。 |
| **前端 UI 界面** | **React + TailwindCSS + Shadcn/UI + ECharts** | 提供顶级现代设计质感：沉浸式暗色/明色主题、流畅动画、双雷达图、会话高亮批注与对比卡片。 |
| **本地后端服务** | **Python (FastAPI )** | 本地随客户端自启动后台服务（`localhost` 端口通信），专职处理文本清洗、大模型 Agent 编排流水线与业务逻辑。 |
| **AI 编排引擎** | **LangChain**  | 负责分步式 Chain/Agent 调度：会话切分与角色识别 → S/D 多维度规则评分 → 一票否决判定 → 话术错误定位 → 金牌话术重写。 |
| **本地持久化存储** | **SQLite + SQLAlchemy** | 零配置单文件数据库（如 `data/app.db`），自动存储员工信息、历史聊天原文、质检报告 JSON 与趋势数据。 |

---

#### 2、 整体架构与数据流转

```
[ 用户操作界面 (Tauri / React 桌面端) ]
   ├── 1. 员工档案管理（新建/切换下属、查看历史评分趋势与雷达图）
   ├── 2. 会话上传与质检触发（支持拖拽 TXT/JSON/CSV 文件或直接粘贴）
   └── 3. 质检结果面板（总分仪表盘 + 双雷达图 + 原话高亮 vs AI黄金改写 + 一键导出 PDF/长图）
         │ 
         │ (HTTP / WebSocket 内部通讯: http://127.0.0.1:8000)
         ▼
[ 本地 Python 后端 (FastAPI/Flask + Agent 编排) ]
   ├── 步骤 1：会话预处理（正则分词、角色 [客]/[助] 识别、基础格式防呆校验）
   ├── 步骤 2：质检 Agent (Scoring Node) -> 依据 S/D 规则执行打分与 59 分熔断计算[cite: 1]
   ├── 步骤 3：修复 Agent (Rewrite Node) -> 抓取扣分话术，调用金牌 SOP 知识库生成高情商改写
   └── 步骤 4：建议 Agent (Advisory Node) -> 汇总失分归因，生成 1~3 条个性化改进建议
         │
         ▼
[ 本地 SQLite 数据库 (app.db) ]
   ├── 表1: assistants (id, name, employee_no, template_type, created_at)
   ├── 表2: inspections (id, assistant_id, original_text, total_score, is_yellow_alert, created_at)
   └── 表3: inspection_details (id, inspection_id, result_json)

```

---

#### 3、 本地 SQLite 核心表结构设计

```sql
-- 1. 下属/投顾助理表
CREATE TABLE assistants (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    employee_no TEXT UNIQUE NOT NULL,
    template_type TEXT DEFAULT 'standard', -- standard(标准), newbie(新人), vip(高净值)
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 2. 质检报告主表
CREATE TABLE inspections (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    assistant_id INTEGER NOT NULL,
    session_title TEXT,
    total_score INTEGER NOT NULL,
    is_yellow_alert BOOLEAN DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY(assistant_id) REFERENCES assistants(id)
);

-- 3. 质检详情存储表 (存储完整的结构化评估数据)
CREATE TABLE inspection_details (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    inspection_id INTEGER UNIQUE NOT NULL,
    raw_dialogue TEXT NOT NULL,          -- 原始聊天文本
    d_scores_json TEXT NOT NULL,          -- D端详细打分与评语
    s_scores_json TEXT NOT NULL,          -- S端详细打分与评语
    highlight_dialogue_json TEXT,         -- 扣分点与 AI 黄金改写对比
    suggestions_json TEXT,                -- 改进建议列表
    FOREIGN KEY(inspection_id) REFERENCES inspections(id)
);

```

---

#### 三、 核心引擎：大模型结构化输出设计 (Prompt Schema)

为了让前端能渲染出丰富、整洁的可视化报告，需要让大模型按**严格的 JSON 格式**输出打分与修复结果：

```json
{
  "total_score": 78,
  "is_yellow_alert": false,
  "yellow_alert_reasons": [],
  "d_scores": {
    "d1_emotion_change": { "score": 10, "rating": "轻微变好", "comment": "客户进场时较焦虑，后半程情绪有所平复。" },
    "d2_profile_match": { "profile": "焦虑型", "score": 6, "match_rating": "完全匹配", "comment": "准确识别了套牢恐慌。" },
    "d3_problem_match": { "score": 8, "surface_vs_deep": "看懂底层", "resolution": "部分匹配", "comment": "捕捉到了资金安全诉求，但方案落地性稍弱。" },
    "d4_expectation_exceed": { "score": 4, "derived_question": 2, "control_given": 2, "comment": "主动预判了解套周期，但未给备选方案。" }
  },
  "s_scores": {
    "s1_emotion_stabilize": { "score": 18, "sub_items": { "empathy": 4, "customized": 5, "direct": 4, "no_conflict": 5, "vent_guide": 0 } },
    "s2_problem_closure": { "score": 11, "sub_items": { "completeness": 3, "structure": 3, "next_step": 2, "follow_up": 3 } },
    "s3_professional_supply": { "score": 5, "sub_items": { "logic": 2, "explain_why": 2, "decision_ownership": 1 } }
  },
  "highlight_dialogue": [
    {
      "turn": 2,
      "role": "助理",
      "original_text": "这个我们也没办法，行情就这样，建议您拿着别看盘了。",
      "issue_type": "共情生硬 / 未给清晰动作 (S1-1, S2-3)",
      "ai_rewrite": "哥，我特别理解您现在的着急，市场连续震荡确实很考验心态。咱们先看这只票的基本面并未受损，建议仓位上先不动，明早 9:30 开盘我帮您盯紧分时做 T 摊薄成本，您看这样行吗？"
    }
  ],
  "improvement_suggestions": [
    "在客户焦虑发泄时，先做情绪承接，切忌用'我也没办法'等推卸式词汇。",
    "给出建议时遵循【结论 + 原因 + 下一步动作】三段论，明确责任归属。"
  ]
}

```

---

### 四、 报告界面 UI 交互设计要点

1. **顶部总览 Card**：
* 醒目的综合评分（如 **82分** / 状态：`正常` 或 `⚠️ 黄灯预警`）。
* 关键标签：`客户画像: 焦虑型`、`质检模板: 标准服务模板`、`耗时: 12轮对话`。


2. **能力维度双雷达图**：
* 左侧 D端雷达（情绪转化、画像匹配、诉求穿透、预期超越）。
* 右侧 S端雷达（情绪维稳、问题闭环、专业供给）。


3. **沉浸式“错误诊断 vs AI 黄金修复”对比折叠面板**：
* 标红指出哪一句话扣了分、违反了哪条规则。
* 下方一键展示 AI 优化后的话术，供主管作为复盘课件直接使用。


4. **一键存档与沉淀**：
* 点击“保存记录”，自动同步至该下属的档案页面，下属页面会自动生成“近 30 天均分走势”和“历史失分最高 Top 3 项”。



---

### 五、 落地推进三步法

1. **第 1 天（打通 Prompt 与打分）**：用 DeepSeek / Claude / GPT API，配合上述 JSON 格式，在 Python 里跑通 10 条导出的真实聊天文本，调优提示词以确保一票否决与分值判断准确。
2. **第 2 天（搭建 Streamlit 界面）**：使用 Cursor 或 Claude 协助生成 Streamlit 前端代码，接入 SQLite 数据库，实现下属增删查改与图表渲染。
3. **第 3 天（投入组内日常实操）**：主管在每周/每日复盘时，导入 5~10 条典型对话直接生成复盘报告，用 AI 改写的话术直接给组员做培训。